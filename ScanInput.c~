#include <stdio.h>

int main()
{
	char iXc, fXc;

	int iXloop = 1;
	int fXloop = 1;

	int iX, iY;
	int fX, fY;
    	int looping = 1;

	while (looping == 1)
	{
		while(iXloop == 1)
		{
			printf("Please enter Initial X (A-H) :");
		   	scanf(" %c", &iXc);
			if( (iXc>='a' && iXc<='h') || (iXc>='A' && iXc<='H'))
		    	{
				if(iXc == 'A' || iXc == 'a')
				{
					iX = 0;
				}
				if(iXc == 'B' || iXc == 'b')
				{
					iX = 1;
				}
				if(iXc == 'C' || iXc == 'c')
				{
					iX = 2;
				}
				if(iXc == 'D' || iXc == 'd')
				{
					iX = 3;
				}
				if(iXc == 'E' || iXc == 'e')
				{
					iX = 4;
				}
				if(iXc == 'F' || iXc == 'f')
				{
					iX = 5;
				}
				if(iXc == 'G' || iXc == 'g')
				{
					iX = 6;
				}
				if(iXc == 'H' || iXc == 'h')
				{
					iX = 7;
				}
				printf("%c is within the range.\n",iXc);
				printf("Initial X value is %d.\n",iX);
				iXloop = 0;
			}
		    	else
		    	{
				printf("%c is not an alphabet or out of the range.\n",iXc);
			}
		}
	        
		while(fXloop == 1)
		{
			printf("Please enter Final X (A-H) :");
			scanf(" %c", &fXc);

			if( (fXc>='a' && fXc<='h') || (fXc>='A' && fXc<='H'))
			{
				if(fXc == 'A' || fXc == 'a')
				{
					fX = 0;
				}
				if(fXc == 'B' || fXc == 'b')
				{
					fX = 1;
				}
				if(fXc == 'C' || fXc == 'c')
				{
					fX = 2;
				}
				if(fXc == 'D' || fXc == 'd')
				{
					fX = 3;
				}
				if(fXc == 'E' || fXc == 'e')
				{
					fX = 4;
				}
				if(fXc == 'F' || fXc == 'f')
				{
					fX = 5;
				}
				if(fXc == 'G' || fXc == 'g')
				{
					fX = 6;
				}
				if(fXc == 'H' || fXc == 'h')
				{
					fX = 7;
				}
				printf("%c is within the range.\n",fXc);
				printf("Final X value is %d.\n",fX);
				fXloop = 0;
			}
			else
			{
				printf("%c is not an alphabet or out of the range.\n",fXc);
			}
		}
		looping = 0;
	}

    return 0;
}
